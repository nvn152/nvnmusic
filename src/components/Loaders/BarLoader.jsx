const BarLoader = ({ title }) => (
  <div className=" w-full h-full object-cover rounded-xl animate-pulse ">
    <div className="h-full w-full bg-gray-200/30 rounded" />
  </div>
);

export default BarLoader;
